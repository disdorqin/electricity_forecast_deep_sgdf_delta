# DeepSGDFDelta / TrendKnight — Requirements
# Core dependencies (PyTorch already in main project requirements.txt)

torch>=2.0
numpy>=1.23
pandas>=1.5
scikit-learn>=1.2
scipy>=1.10
pyyaml>=6.0
openpyxl>=3.0
pytest>=7.0
